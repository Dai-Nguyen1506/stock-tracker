services:
  # ── 1. DATABASE ──────────────────────────────────────
  cassandra:
    image: cassandra:4.1
    environment:
      CASSANDRA_CLUSTER_NAME: "StockCluster"
      CASSANDRA_DC: "datacenter1"
      CASSANDRA_ENDPOINT_SNITCH: "GossipingPropertyFileSnitch"
      MAX_HEAP_SIZE: "512M"   # Giới hạn RAM cho JVM
      HEAP_NEWSIZE: "128M"
    volumes:
      - cassandra_data:/var/lib/cassandra   # Data persist sau khi container stop
    networks:
      - stock_network
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'describe keyspaces' || exit 1"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 60s   # Cassandra khởi động chậm, chờ 60s trước khi check

  # ── 1.1 SCHEMA INIT ──────────────────────────────────
  cassandra-init:
    image: cassandra:4.1
    depends_on:
      cassandra:
        condition: service_healthy
    volumes:
      - ./cassandra/init.cql:/init.cql:ro
    networks:
      - stock_network
    entrypoint: ["/bin/bash", "-c"]
    command: >
      "set -e;
       until cqlsh cassandra 9042 -e 'describe keyspaces'; do
         echo 'Waiting for Cassandra to accept CQL...';
         sleep 5;
       done;
       cqlsh cassandra 9042 -f /init.cql;
       echo 'Cassandra schema initialized.'"
    restart: "no"

  # ── 2. BACKEND ───────────────────────────────────────
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      CASSANDRA_HOST: cassandra   # Tên service = hostname trong Docker network
      APP_ENV: production
      UVICORN_WORKERS: "2"
    depends_on:
      cassandra-init:
        condition: service_completed_successfully
    networks:
      - stock_network
    restart: unless-stopped
    init: true
    stop_grace_period: 30s
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 20s
    logging:
      options:
        max-size: "10m"
        max-file: "3"
    command: ["/bin/sh", "-c", "uvicorn main:app --host 0.0.0.0 --port 8000 --workers ${UVICORN_WORKERS:-2}"]

networks:
  stock_network:
    driver: bridge

# ── VOLUMES ──────────────────────────────────────────
volumes:
  cassandra_data:   # Named volume: Docker quản lý, data không mất khi restart